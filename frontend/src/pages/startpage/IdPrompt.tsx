import React from 'react';
import styled from 'styled-components';

export default function IdPrompt() {
    return (
        <PromptContainer>
            

        </PromptContainer>
    )
}

const PromptContainer = styled.div`
  display: flex;
  flex-direction: column;
`

const PromtText = styled.div`
  font-family: "Gotham Book";
  font-size: 20px;
  padding: 10px;
  line-height: 1.5em;
`
